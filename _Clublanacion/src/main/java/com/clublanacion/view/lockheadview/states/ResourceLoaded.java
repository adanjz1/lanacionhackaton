package com.clublanacion.view.lockheadview.states;

import com.clublanacion.view.lockheadview.LockHeadView;

public class ResourceLoaded implements LockScreenState {

    public ResourceLoaded(LockHeadView view) {
        if (view.getState() instanceof ScreenOnState) {

        }
    }

}
